language: php

php:
  - 7.3

before_script:
  - cp .env.travis .env
  - composer self-update
  - composer install --prefer-source --no-interaction --dev
  - php artisan key:generate --force
  - php artisan jwt:secret --force
  - php artisan migrate --seed --force

before_install:
  - mysql -e 'CREATE DATABASE cart;'

script:
  - phpunit

services:
  - mysql

notifications:
  email: false
